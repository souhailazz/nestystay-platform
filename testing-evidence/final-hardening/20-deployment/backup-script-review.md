The reviewed `scripts/backup-postgres.sh` and `scripts/backup-object-storage.sh` use strict bash mode, custom/compressed formats, restrictive umask, checksum sidecars and retention pruning. Git Bash `bash -n` passed for both scripts. Remote restic upload is opt-in and refuses to run without an explicit repository and password file. No remote destination was claimed or configured in this environment.
