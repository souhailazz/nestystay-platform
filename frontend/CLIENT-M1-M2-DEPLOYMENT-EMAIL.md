# Client handoff reference

The complete client email is stored in the backend repository at
`CLIENT-M1-M2-DEPLOYMENT-EMAIL.md`. This frontend repository contains the
browser-specific deployment instructions in `DEPLOYMENT.md` and must be
configured with public `VITE_*` build variables only. Backend secrets belong in
the backend server secret manager.
