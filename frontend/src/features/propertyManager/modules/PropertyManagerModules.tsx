import { useState, type Dispatch, type SetStateAction } from "react";
import { BarChart3, CalendarDays, CheckCircle2, CreditCard, FileText, ShieldCheck, Wrench } from "lucide-react";
import { AppLink } from "../../../components/AppLink";
import { Button } from "../../../components/ui/Button";
import { Card } from "../../../components/ui/Card";
import { EmptyState } from "../../../components/ui/EmptyState";
import { Field, Input, Select } from "../../../components/ui/Input";
import { StatusChip } from "../../../components/ui/StatusChip";
import { api, formatMoney, type PropertyManagerDashboard, type PropertyManagerReport } from "../../../lib/api";

export type PropertyManagerModule = "invoices" | "payments" | "utilities" | "maintenance" | "vendors" | "community" | "governance" | "documents" | "gates" | "reports" | "subscription" | "calendar" | "work-orders" | "agreements" | "approvals" | "team" | "inspections" | "cleaning" | "verification" | "insurance";
type Payments = Awaited<ReturnType<typeof api.getPropertyManagerPayments>>;
type CalendarRows = Awaited<ReturnType<typeof api.getPropertyManagerCalendar>>;
type SubscriptionEvents = Awaited<ReturnType<typeof api.getPropertyManagerSubscriptionEvents>>;
type QrRows = Awaited<ReturnType<typeof api.listPropertyManagerQrs>>;
type GateDelivery = Awaited<ReturnType<typeof api.getPropertyManagerGateDelivery>>;

export type PropertyManagerModuleProps = {
  module: PropertyManagerModule;
  title: string;
  data: PropertyManagerDashboard;
  token: string;
  report: PropertyManagerReport | null;
  busy: boolean;
  run: (action: () => Promise<unknown>) => Promise<void>;
  selectedOwner: string;
  setOwner: Dispatch<SetStateAction<string>>;
  selectedProperty: string;
  setProperty: Dispatch<SetStateAction<string>>;
  ownerName: string;
  workScope: string;
  setWorkScope: Dispatch<SetStateAction<string>>;
  subscriptionAction: string;
  setSubscriptionAction: Dispatch<SetStateAction<string>>;
  subscriptionTargetTier: string;
  setSubscriptionTargetTier: Dispatch<SetStateAction<string>>;
  subscriptionReason: string;
  setSubscriptionReason: Dispatch<SetStateAction<string>>;
  subscriptionEvents: SubscriptionEvents;
  setSubscriptionEvents: Dispatch<SetStateAction<SubscriptionEvents>>;
  qrs: QrRows;
  qrReason: string;
  setQrReason: Dispatch<SetStateAction<string>>;
  gateDelivery: Record<string, GateDelivery>;
  setGateDelivery: Dispatch<SetStateAction<Record<string, GateDelivery>>>;
  selectedPayments: Payments;
  calendar: CalendarRows;
  comment: string;
  setComment: Dispatch<SetStateAction<string>>;
};

function InvoicesModule({ data, report, busy, run, token }: PropertyManagerModuleProps) {
  return <>
    <Card className="mb-6"><div className="flex items-center gap-2"><BarChart3 size={18} /><h2 className="m-0 font-display text-2xl">Real portfolio totals</h2></div>{report ? <div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div>Invoice revenue<strong className="block">{formatMoney(report.grossInvoiceRevenue)}</strong></div><div>Payments<strong className="block">{formatMoney(report.paymentRevenue)}</strong></div><div>Maintenance spend<strong className="block">{formatMoney(report.maintenanceSpend)}</strong></div><div>Utility revenue<strong className="block">{formatMoney(report.utilityRevenue)}</strong></div></div> : <p className="mt-3 text-sm text-sand-600">Report totals are not available yet.</p>}</Card>
    <Card className="mb-6"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="m-0 font-display text-2xl">Invoice operations</h2><div className="flex flex-wrap gap-2"><Button disabled={busy || data.invoices.length === 0} variant="outline" onClick={() => void run(() => api.markPropertyManagerInvoicesOverdue(token))}>Mark overdue &amp; queue reminders</Button><Button disabled={busy || data.invoices.length === 0} onClick={() => void run(() => api.bulkIssuePropertyManagerInvoices(token, data.invoices.filter((invoice) => invoice.status === "DRAFT").map((invoice) => invoice.id)))}>Issue drafts in bulk</Button></div></div>{data.invoices.length === 0 ? <EmptyState title="No invoices" /> : <div className="mt-3 grid gap-2">{data.invoices.map((invoice) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3" key={invoice.id}><div><strong>{invoice.invoiceNumber}</strong><p className="m-0 text-xs text-sand-600">Due {invoice.dueDate} · balance {formatMoney(invoice.balance)}</p></div><StatusChip value={invoice.status} /></div>)}</div>}</Card>
  </>;
}

function PaymentsModule({ selectedPayments, busy, run, token }: PropertyManagerModuleProps) {
  return <Card className="mb-6"><div className="flex items-center gap-2"><CreditCard size={18} /><h2 className="m-0 font-display text-2xl">Payments and reconciliation</h2></div>{selectedPayments.length === 0 ? <EmptyState title="No payment records" copy="Captured, failed and refunded payment attempts appear here when recorded." /> : <div className="mt-4 grid gap-2">{selectedPayments.map((payment) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3" key={payment.id}><div><strong>{formatMoney(payment.amount)}</strong><p className="m-0 text-xs text-sand-600">{payment.provider} · {payment.reconciliationStatus} · {new Date(payment.createdAt).toLocaleString()}</p></div><div className="flex gap-2"><StatusChip value={payment.status} />{payment.status === "FAILED" && <Button disabled={busy} variant="outline" onClick={() => void run(() => api.retryPropertyManagerPayment(token, payment.id))}>Retry</Button>}{payment.status === "CAPTURED" && <Button disabled={busy} variant="outline" onClick={() => void run(() => api.refundPropertyManagerPayment(token, payment.id, { reason: "Manager requested refund", idempotencyKey: crypto.randomUUID() }))}>Refund</Button>}</div></div>)}</div>}</Card>;
}

function UtilitiesModule({ data, busy, run, token, selectedOwner, setOwner, selectedProperty, setProperty }: PropertyManagerModuleProps) {
  const [meter, setMeter] = useState({ previous: "0", current: "0", rate: "0" });
  return <Card className="mb-6"><h2 className="font-display text-2xl">Meter readings and anomaly detection</h2><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Field label="Owner"><Select value={selectedOwner} onChange={(event) => setOwner(event.target.value)}>{data.owners.map((owner) => <option key={owner.ownerUserId} value={owner.ownerUserId}>{owner.displayName}</option>)}</Select></Field><Field label="Property"><Select value={selectedProperty} onChange={(event) => setProperty(event.target.value)}>{data.properties.map((property) => <option key={property.id} value={property.id}>{property.title} · {property.unitNumber}</option>)}</Select></Field>{(["previous", "current", "rate"] as const).map((key) => <Field key={key} label={key[0].toUpperCase() + key.slice(1)}><Input inputMode="decimal" value={meter[key]} onChange={(event) => setMeter((current) => ({ ...current, [key]: event.target.value }))} /></Field>)}</div><Button className="mt-3" disabled={busy || !selectedOwner || !selectedProperty} onClick={() => void run(() => api.recordPropertyManagerMeterReading(token, { ownerUserId: selectedOwner, propertyId: selectedProperty, utilityType: "WATER", billingPeriod: new Date().toISOString().slice(0, 7), previousReading: Number(meter.previous), currentReading: Number(meter.current), rate: Number(meter.rate) }))}>Record reading</Button><div className="mt-4 grid gap-2">{data.utilities.filter((item) => item.propertyId === selectedProperty).map((item) => <div className="rounded-field border border-sand-border p-3" key={item.id}><strong>{item.utilityType} · {item.billingPeriod}</strong><p className="m-0 text-sm">{item.usage} × {formatMoney(item.rate)} = {formatMoney(item.amount)}</p></div>)}</div></Card>;
}

function MaintenanceModule({ data, busy, run, token, ownerName, selectedProperty, selectedOwner, workScope, setWorkScope }: PropertyManagerModuleProps) {
  return <Card className="mb-6"><h2 className="font-display text-2xl">Maintenance and SLA workflow</h2>{data.maintenance.length === 0 ? <EmptyState title="No maintenance requests" copy="Owner requests and manager work updates will appear here." /> : <div className="mt-4 grid gap-2">{data.maintenance.map((item) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3" key={item.id}><div><strong>{item.title}</strong><p className="m-0 text-xs text-sand-600">{item.category} · {item.urgency} · {formatMoney(item.cost)}</p></div><div className="flex gap-2"><StatusChip value={item.status} /><Button disabled={busy || item.status === "COMPLETED"} variant="outline" onClick={() => void run(() => api.updatePropertyManagerMaintenance(token, item.id, { status: "COMPLETED", cost: item.cost, notes: item.notes }))}>Complete</Button></div></div>)}</div>}<div className="mt-4 grid gap-2 sm:grid-cols-[1fr_auto]"><Input aria-label="New work order scope" placeholder={`Create work order for ${ownerName}`} value={workScope} onChange={(event) => setWorkScope(event.target.value)} /><Button disabled={busy || !workScope || !selectedProperty} onClick={() => void run(() => api.createPropertyManagerWorkOrder(token, { propertyId: selectedProperty, ownerUserId: selectedOwner, scope: workScope }))}><Wrench size={16} /> Create work order</Button></div></Card>;
}

function CalendarModule({ calendar }: PropertyManagerModuleProps) {
  return <Card className="mb-6"><div className="flex items-center gap-2"><CalendarDays size={18} /><h2 className="m-0 font-display text-2xl">Master calendar</h2></div>{calendar.length === 0 ? <EmptyState title="Calendar is clear" copy="Bookings, maintenance, cleaning and inspections will be synchronized here." /> : <div className="mt-4 grid gap-2">{calendar.map((item) => <div className="rounded-field border border-sand-border p-3" key={item.id}><strong>{item.title}</strong><p className="m-0 text-xs text-sand-600">{item.eventType} · {new Date(item.startsAt).toLocaleString()} – {new Date(item.endsAt).toLocaleString()}</p></div>)}</div>}</Card>;
}

function SubscriptionModule({ data, busy, run, token, subscriptionAction, setSubscriptionAction, subscriptionTargetTier, setSubscriptionTargetTier, subscriptionReason, setSubscriptionReason, subscriptionEvents, setSubscriptionEvents }: PropertyManagerModuleProps) {
  return <Card className="mb-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="m-0 font-display text-2xl">Subscription lifecycle</h2><p className="m-0 mt-1 text-sm text-sand-600">Server-authoritative billing state with pause, renewal, downgrade scheduling, retry and cancellation recovery.</p></div><StatusChip value={data.manager.subscriptionStatus} /></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-field bg-shell p-3"><span className="text-xs text-sand-600">Current plan</span><strong className="mt-1 block">{data.manager.subscriptionTier}</strong><span className="text-xs text-sand-500">{formatMoney(data.manager.monthlyAmount)} / month</span><span className="mt-1 block text-xs text-sand-500">Provider: {data.manager.billingProviderStatus}</span></div><div className="rounded-field bg-shell p-3"><span className="text-xs text-sand-600">Units</span><strong className="mt-1 block">{data.manager.unitsUsed}{data.manager.unitLimit == null ? "" : ` / ${data.manager.unitLimit}`}</strong></div><div className="rounded-field bg-shell p-3"><span className="text-xs text-sand-600">Next billing</span><strong className="mt-1 block">{new Date(data.manager.nextBillingAt).toLocaleDateString()}</strong></div><div className="rounded-field bg-shell p-3"><span className="text-xs text-sand-600">Auto-renew</span><strong className="mt-1 block">{data.manager.autoRenew ? "On" : "Off"}</strong><Button className="mt-2" disabled={busy} variant="outline" onClick={() => void run(() => api.changePropertyManagerSubscription(token, { action: "AUTO_RENEW", autoRenew: !data.manager.autoRenew, reason: "Manager updated auto-renew preference" }))}>{data.manager.autoRenew ? "Turn off auto-renew" : "Turn on auto-renew"}</Button></div></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-[auto_auto_1fr_auto]" ><Field label="Lifecycle action"><Select value={subscriptionAction} onChange={(event) => setSubscriptionAction(event.target.value)}><option>PAUSE</option><option>RESUME</option><option>UPGRADE</option><option>DOWNGRADE</option><option>CANCEL</option><option>REACTIVATE</option></Select></Field>{(subscriptionAction === "UPGRADE" || subscriptionAction === "DOWNGRADE") && <Field label="Target plan"><Select value={subscriptionTargetTier} onChange={(event) => setSubscriptionTargetTier(event.target.value)}><option>Standard</option><option>Professional</option><option>Enterprise</option></Select></Field>}<Field label="Reason"><Input value={subscriptionReason} onChange={(event) => setSubscriptionReason(event.target.value)} placeholder="Why is this change needed?" /></Field><Button className="self-end" disabled={busy || (subscriptionAction === "CANCEL" && !subscriptionReason.trim())} onClick={() => void run(async () => { const updated = await api.changePropertyManagerSubscription(token, { action: subscriptionAction, targetTier: subscriptionAction === "UPGRADE" || subscriptionAction === "DOWNGRADE" ? subscriptionTargetTier : undefined, reason: subscriptionReason.trim() || "Manager initiated from workspace" }); setSubscriptionReason(""); return updated; })}>Apply change</Button></div><div className="mt-5 flex items-center justify-between gap-2"><h3 className="m-0 font-display text-lg">Billing history</h3><Button disabled={busy} variant="outline" onClick={() => void api.getPropertyManagerSubscriptionEvents(token).then(setSubscriptionEvents)}>Refresh history</Button></div>{subscriptionEvents.length === 0 ? <EmptyState title="No billing events yet" /> : <div className="mt-3 grid gap-2">{subscriptionEvents.slice(0, 12).map((event) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3 text-sm" key={event.id}><div><strong>{event.eventType.replaceAll("_", " ")}</strong><p className="m-0 mt-1 text-xs text-sand-600">{event.fromTier} → {event.toTier} · {event.reason ?? "No reason provided"}</p></div><StatusChip value={event.status} /></div>)}</div>}</Card>;
}

function GatesModule({ data, busy, run, token, qrs, qrReason, setQrReason, gateDelivery, setGateDelivery }: PropertyManagerModuleProps) {
  const firstGate = data.gateMessages[0];
  return <><Card className="mb-6"><div className="flex items-center gap-2"><CheckCircle2 size={18} /><h2 className="m-0 font-display text-2xl">Gate communications</h2></div>{data.gateMessages.length === 0 ? <EmptyState title="No gate messages" copy="Gate delivery history will appear here when messages are sent." /> : <div className="mt-4 grid gap-2">{data.gateMessages.map((message) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3" key={message.id}><span>{message.recipient}: {message.message}</span><div className="flex gap-2"><StatusChip value={message.visitorType} />{message.id === firstGate?.id && <Button disabled={busy} variant="outline" onClick={() => void api.getPropertyManagerGateDelivery(token, message.id).then((result) => setGateDelivery((current) => ({ ...current, [message.id]: result })))}>View delivery</Button>}</div>{gateDelivery[message.id]?.map((attempt) => <span className="w-full text-xs text-sand-600" key={attempt.id}>Attempt {attempt.attemptNumber}: {attempt.status}</span>)}</div>)}</div>}</Card><Card className="mb-6"><h2 className="font-display text-2xl">QR lifecycle</h2>{qrs.length === 0 ? <EmptyState title="No issued QR codes" copy="Issue a guest or vendor QR to see active, expired and revoked states here." /> : <div className="mt-3 grid gap-2">{qrs.map((qr) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3" key={qr.id}><div><strong>{qr.subjectType}</strong><p className="m-0 text-xs text-sand-600">Expires {new Date(qr.validUntil).toLocaleString()} · {qr.status}</p></div><div className="flex flex-wrap items-center gap-2"><StatusChip value={qr.status} />{qr.status === "ACTIVE" && <><Input aria-label={`Revoke reason for ${qr.id}`} value={qrReason} onChange={(event) => setQrReason(event.target.value)} /><Button disabled={busy || !qrReason.trim()} variant="outline" onClick={() => void run(() => api.revokePropertyManagerQr(token, qr.id, qrReason))}>Revoke</Button></>}</div></div>)}</div>}</Card></>;
}

function ReportsModule({ data, report }: PropertyManagerModuleProps) {
  return <><Card className="mb-6"><div className="flex items-center gap-2"><BarChart3 size={18} /><h2 className="m-0 font-display text-2xl">Real portfolio totals</h2></div>{report ? <div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div>Invoice revenue<strong className="block">{formatMoney(report.grossInvoiceRevenue)}</strong></div><div>Payments<strong className="block">{formatMoney(report.paymentRevenue)}</strong></div><div>Maintenance spend<strong className="block">{formatMoney(report.maintenanceSpend)}</strong></div><div>Utility revenue<strong className="block">{formatMoney(report.utilityRevenue)}</strong></div></div> : <EmptyState title="Report unavailable" copy="Refresh to retry the portfolio report." />}</Card><Card><div className="flex items-center gap-2"><FileText size={18} /><h2 className="m-0 font-display text-2xl">Portfolio activity</h2></div>{data.maintenance.length === 0 && data.invoices.length === 0 ? <EmptyState title="No activity yet" copy="Create an owner, property, invoice or request to begin the manager workflow." /> : <div className="mt-4 grid gap-2">{data.invoices.slice(0, 5).map((invoice) => <div className="flex justify-between rounded-field border border-sand-border p-3" key={invoice.id}><span>{invoice.invoiceNumber}</span><strong>{formatMoney(invoice.balance)}</strong></div>)}</div>}</Card></>;
}

function InsuranceModule({ data }: PropertyManagerModuleProps) {
  return <Card className="mb-6"><div className="flex items-center gap-2"><ShieldCheck size={18} /><h2 className="m-0 font-display text-2xl">Coverage readiness</h2></div><p className="mt-2 text-sm text-sand-600">This view is backed by the manager portfolio API. It reports only listing-level coverage metadata when the API exposes it; it does not invent manager plans, prices, or policy limits.</p>{data.properties.length === 0 ? <EmptyState title="No managed properties" copy="Add a managed property before reviewing coverage readiness." /> : <div className="mt-4 grid gap-2">{data.properties.map((property) => { const status = property.insuraGuestEnabled == null ? "Not exposed by API" : property.insuraGuestEnabled ? "Enabled" : "Not enabled"; return <div className="flex flex-wrap items-center justify-between gap-3 rounded-field border border-sand-border p-3" key={property.id}><div><strong>{property.title}</strong><p className="m-0 text-xs text-sand-600">{property.address} · unit {property.unitNumber}</p></div><StatusChip value={status} /></div>; })}</div>}<p className="m-0 mt-4 text-xs text-sand-500">Policy purchase and plan administration remain provider-contract work; listing eligibility is configured from the canonical property workflow.</p></Card>;
}

function PortfolioModule({ module, title, data, busy, run, token, comment, setComment }: PropertyManagerModuleProps) {
  const action = module === "vendors" && data.vendors[0] ? <Button className="mt-2" disabled={busy} variant="outline" onClick={() => void run(() => api.updatePropertyManagerVendor(token, data.vendors[0].id, { isPreferred: !data.vendors[0].isPreferred }))}>Toggle preferred</Button> : module === "documents" && data.documents[0] ? <Button className="mt-2" disabled={busy} variant="outline" onClick={() => void run(() => api.archivePropertyManagerDocument(token, data.documents[0].id))}>Archive first</Button> : module === "governance" && data.proposals.find((item) => item.status === "OPEN") ? <Button className="mt-2" disabled={busy} variant="outline" onClick={() => void run(() => api.closePropertyManagerProposal(token, data.proposals.find((item) => item.status === "OPEN")!.id))}>Close open proposal</Button> : null;
  return <Card className="mb-6"><div className="flex items-center gap-2"><CheckCircle2 size={18} /><h2 className="m-0 font-display text-2xl">{title}</h2></div><p className="mt-2 text-sm text-sand-600">Live records are loaded from the manager API with server-side ownership checks.</p><div className="mt-4 grid gap-2 sm:grid-cols-2"><div className="rounded-field bg-shell p-3"><strong>{data.vendors.length}</strong><p className="m-0 text-xs text-sand-600">vendors in portfolio</p>{action}</div><div className="rounded-field bg-shell p-3"><strong>{data.documents.length}</strong><p className="m-0 text-xs text-sand-600">documents with object-storage metadata</p></div><div className="rounded-field bg-shell p-3"><strong>{data.notices.length}</strong><p className="m-0 text-xs text-sand-600">community notices</p>{module === "community" && data.notices[0] && <div className="mt-2 flex gap-2"><Input aria-label="Notice comment" value={comment} onChange={(event) => setComment(event.target.value)} placeholder="Add comment" /><Button disabled={busy || !comment.trim()} onClick={() => void run(async () => { await api.addPropertyManagerNoticeComment(token, data.notices[0].id, comment); setComment(""); })}>Comment</Button></div>}</div><div className="rounded-field bg-shell p-3"><strong>{data.proposals.length}</strong><p className="m-0 text-xs text-sand-600">governance proposals</p></div></div><AppLink className="mt-4 inline-flex font-semibold underline" href="/pm/dashboard">Open full manager dashboard</AppLink></Card>;
}

export function PropertyManagerModuleContent(props: PropertyManagerModuleProps) {
  switch (props.module) {
    case "invoices": return <InvoicesModule {...props} />;
    case "payments": return <PaymentsModule {...props} />;
    case "utilities": return <UtilitiesModule {...props} />;
    case "maintenance": return <MaintenanceModule {...props} />;
    case "calendar": return <CalendarModule {...props} />;
    case "subscription": return <SubscriptionModule {...props} />;
    case "gates": return <GatesModule {...props} />;
    case "reports": return <ReportsModule {...props} />;
    case "insurance": return <InsuranceModule {...props} />;
    default: return <PortfolioModule {...props} />;
  }
}
